sorted(variable) - sorts given array, string etc.
hashsetName = set() - initializes an empty HashSet
hashmapName = {} - initializes an empty HashMap
enumerate(variable) - adds a counter to each item in the list, array etc. turns iterable into something we can loop through, each item comes with its number
= - assigns a value to variable
== - comparison between variables
=== - 
defaultdict(list) - defining a dictionary with values as a list, special kind of dictionary that provides a default value for key that does not exist, never raises key error
ord() function - returns the number representing the unicode code of a specified character